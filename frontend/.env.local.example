# Only read on the server — never shipped to the browser, which is why the API needs no CORS.
# Inside Compose the `web` service overrides this with http://production-api:8080.
API_BASE_URL=http://localhost:5033
